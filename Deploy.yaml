apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: express
  name: basic-express-app-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: express
  template:
    metadata:
      labels:
        app: express
    spec:
      containers:
      - image: manojjahgirdar/gitops-lifecycle-tekton:v54659365-ac33-4a58-aba3-e61477944cd8
        name: gitops-demo
        ports:
        - containerPort: 3000
