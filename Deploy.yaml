apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: express
  name: basic-express-app-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: express
  template:
    metadata:
      labels:
        app: express
    spec:
      containers:
      - image: manojjahgirdar/gitops-lifecycle-tekton:v118da289-d442-44cf-b24b-1d7bd098cdab
        name: gitops-demo
        ports:
        - containerPort: 3000
